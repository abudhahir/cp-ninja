# API Design: [HTTP_METHOD] [PATH]

> Generated by API Design Helper | OpenAPI 3.0.3 | Spring Boot / SpringDoc

---

## Overview

| Field        | Value                          |
|--------------|--------------------------------|
| Method       | [HTTP_METHOD]                  |
| Path         | [FULL_PATH]                    |
| Version      | [API_VERSION]                  |
| Base URL     | [BASE_URL]                     |
| Content-Type | application/json               |
| Idempotent   | [YES / NO / N/A]               |

**Purpose:** [ENDPOINT_DESCRIPTION]

---

## Request

### Path Parameters

| Name | Type | Required | Description |
|------|------|----------|-------------|
| [name] | [type] | Yes | [description] |

*(N/A if none)*

### Query Parameters

| Name | Type | Required | Default | Description |
|------|------|----------|---------|-------------|
| [name] | [type] | [Yes/No] | [value/none] | [description] |

*(N/A if none)*

### Request Body

| Field | Type | Required | Constraints | Description |
|-------|------|----------|-------------|-------------|
| [field] | [type] | [Yes/No] | [constraints] | [description] |

*(N/A if no request body)*

---

## Response

### Success Response — [SUCCESS_STATUS_CODE] [STATUS_TEXT]

| Field | Type | Description |
|-------|------|-------------|
| [field] | [type] | [description] |

*(N/A if no response body)*

### Error Responses

| Status Code | Reason | Message |
|-------------|--------|---------|
| [code] | [reason] | [message] |

*(Populate with actual error codes from the design interview only. Do not include codes that don't apply.)*

---

## Validation Rules

| Field | Constraint | Message |
|-------|-----------|---------|
| [field] | [annotation] | [message] |

*(N/A if no request body)*

---

## Caching & Versioning Notes

- **Versioning strategy:** [URL_PATH / REQUEST_HEADER / ACCEPT_HEADER / NONE]
- **Cache-Control:** [value or "No caching"]
- **Rate limiting:** [description or "None documented"]
- **Additional notes:** [any edge cases or special behaviors]

---

## Java Code Headers

### JavaDoc Comment Block

```java
/**
 * [ENDPOINT_DESCRIPTION]
 *
 * <p>Endpoint: [HTTP_METHOD] [FULL_PATH]</p>
 * <p>API Version: [API_VERSION]</p>
 *
 * @param [param_name] [description]
 * @param requestBody the {@link [BodyClassName]} containing the request data
 * @return [RESPONSE_DESCRIPTION] ([SUCCESS_STATUS_CODE] [STATUS_TEXT])
 * @throws ResponseStatusException [ERROR_STATUS_CODE] if [reason]
 */
```

> Repeat one `@param` line per path/query parameter. Repeat one `@throws` line per error code.

### SpringDoc Annotations

```java
@Operation(
    summary = "[SHORT_SUMMARY]",
    description = "[ENDPOINT_DESCRIPTION]"
)
@ApiResponses(value = {
    @ApiResponse(
        responseCode = "[SUCCESS_STATUS_CODE]",
        description = "[SUCCESS_DESCRIPTION]",
        content = @Content(mediaType = "application/json",
            schema = @Schema(implementation = [ResponseClassName].class))
    ),
    @ApiResponse(responseCode = "[ERROR_CODE]", description = "[REASON]: [MESSAGE]")
})
@Parameter(name = "[name]", description = "[description]", required = [true/false],
    in = ParameterIn.[PATH/QUERY])
```

> `ParameterIn` import: `io.swagger.v3.oas.annotations.enums.ParameterIn`
> Place `@Parameter` on method parameters in the Java signature, not floating before the method.
> `[ResponseClassName]` and `[ResponseSchema]` must use the same base name.
> `[BodyClassName]` and `[RequestBodySchema]` must use the same base name.

---

## OpenAPI 3.0 Spec

```yaml
openapi: "3.0.3"
info:
  title: "[ENDPOINT_DESCRIPTION] API"
  version: "[API_VERSION]"

paths:
  [FULL_PATH]:
    [http_method_lowercase]:
      summary: "[SHORT_SUMMARY]"
      description: "[ENDPOINT_DESCRIPTION]"
      operationId: "[camelCaseOperationName]"
      parameters:
        - name: "[param_name]"
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/[RequestBodySchema]'
      responses:
        "[SUCCESS_STATUS_CODE]":
          description: "[SUCCESS_DESCRIPTION]"
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/[ResponseSchema]'
        "[ERROR_CODE]":
          description: "[ERROR_REASON]"

components:
  schemas:
    [RequestBodySchema]:
      type: object
      required:
        - [required_field]
      properties:
        [field_name]:
          type: [string/integer/boolean/array/object]
          description: "[description]"
    [ResponseSchema]:
      type: object
      properties:
        [field_name]:
          type: [string/integer/boolean/array/object]
          description: "[description]"
```

> Internal `$ref` to `#/components/schemas/...` is correct. Do NOT use external file refs (e.g. `$ref: './other.yaml'`).
